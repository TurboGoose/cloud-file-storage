spring:
  data:
    redis:
      host: redis

  datasource:
    username: ilya
    password: bebra
    url: jdbc:postgresql://postgres:5432/cloud-storage

  servlet:
    multipart:
      max-file-size: 20MB
      max-request-size: 20MB

  session:
    timeout: 1D

minio:
  endpoint: http://minio:9000
  username: ilya
  password: bebrabebra